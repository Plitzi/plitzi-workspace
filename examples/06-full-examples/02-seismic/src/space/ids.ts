/**
 * The elements a flow on ANOTHER element calls, by id — named once, here, so the modules that author them and the
 * ones that call them need not import each other.
 */
export const FULLSCREEN_ID = 'fullscreen-toggle';

export const NOTIFIER_ID = 'notifier';
