# Pizarra — a collaborative whiteboard

An infinite board in a hand-drawn stroke, for drawing, planning and deciding together. Shapes (rectangles, ellipses,
diamonds, triangles, hexagons, cylinders, stars), arrows fixed to what they connect, a pen with eight brushes, text,
sticky notes, task cards, frames that hold what is put in them — a column frame is a kanban lane — pictures, and
comments with threads. Styled the way Excalidraw styles them: stroke, background, fill style, width, stroke style,
sloppiness, edges, opacity and layers.

Send the link and everyone on the board sees every shape, cursor, drag and rename the moment it happens: live cursors
with names, avatars of who is here, a chat kept with the board, cursor chat, reactions, a laser, votes, a shared
timer, "bring everyone here", following a collaborator's view, presenting the frames one by one, a minimap with where
everyone is. And **AI agents join as collaborators**: an agent appears on the board by name, with a cursor the people
watch move, reads what is there, adds to it and talks in the chat.

Boards are public or private, kept for good or made temporary — gone for everyone after an hour, five, a day or a
week — can be locked with a password, and run on one process or on several replicas behind a balancer.

```bash
yarn workspace @plitzi/example-whiteboard start
# http://127.0.0.1:4016/ — then open the same board in a second window
```

No account, no database. One process keeps the boards and their pictures in memory: a restart clears them and draws
the featured boards again. [Several replicas](#several-replicas) share them through Redis.

---

## What this example is for

The others read data and show it. This one is about **pages that see each other**: realtime channels, the pub/sub
adapter behind them, and the split between what must be validated and kept (a shape) and what only has to be fast
(a cursor). It is the example to read before building anything multiplayer.

| File                                                                                            | What it is                                                                                                                                                                                                                                                                                         |
| ----------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [`src/board/model.ts`](./src/board/model.ts)                                                    | What a board is made of, read by the browser, the server and the agent alike: the element types, their styles, their validation, the merge                                                                                                                                                         |
| [`src/board/store.ts`](./src/board/store.ts)                                                    | Boards kept in the action `kv`: one value per board, a preview per board, its chat, the list the front page reads — every write under one lock that holds across replicas                                                                                                                          |
| [`src/board/locks.ts`](./src/board/locks.ts)                                                    | A board's password: a scrypt hash, and the key and secret topic opening it hands out, signed with the deployment's secret                                                                                                                                                                          |
| [`src/board/assets.ts`](./src/board/assets.ts)                                                  | Pasted pictures: checked by their bytes, capped per picture and per board, kept in memory or in Redis                                                                                                                                                                                              |
| [`src/board/templates.ts`](./src/board/templates.ts) · [`featured.ts`](./src/board/featured.ts) | What a new board starts as, and the two boards drawn at start                                                                                                                                                                                                                                      |
| [`src/deployment.ts`](./src/deployment.ts)                                                      | One process or one replica of several: where the boards, the pictures and the channels live, and what keys are signed with                                                                                                                                                                         |
| [`src/tasks.ts`](./src/tasks.ts) · [`src/actions.ts`](./src/actions.ts)                         | The `board.*` tasks, and the actions that run them — each change ends with `realtime.publish`                                                                                                                                                                                                      |
| [`src/space/`](./src/space)                                                                     | The front page (`home/`) and the board page: toolbar, style panel, people, chat, frames, reach, timer, password, keys                                                                                                                                                                              |
| [`src/plugins/Board/`](./src/plugins/Board)                                                     | The canvas, one module per concern over a shared `core.ts`: `pointer.ts` (gestures), `render.ts` and `draw.ts` (a frame), `picking.ts`, `containers.ts` (frames and columns), `styling.ts`, `pens.ts` (brushes), `quick.ts`, `minimap.ts`, `remotes.ts`, `demo.ts`; `controller.ts` assembles them |
| [`src/plugins/`](./src/plugins)                                                                 | The other elements: `StickyStack` (the tray's pile), `Countdown` (a shared clock), `ShareCard` (QR code and link), `CopyText` (a text and a button that copies it)                                                                                                                                 |
| [`src/agent/`](./src/agent)                                                                     | The MCP server an agent joins a board through — a client of the board's server like a browser is                                                                                                                                                                                                   |
| [`src/main.ts`](./src/main.ts)                                                                  | The server: `action` for the boards, `realtime` for the channels, a middleware for the pictures and the mark                                                                                                                                                                                       |

---

## Two roads

```text
         pointer, drag, selection  ──▶  room:{id}   (publish: 'clients', presence)  ──▶  every other page
 page ──┤
         a commit (onCommit)       ──▶  board-apply ─▶ validate · merge · keep ─▶ realtime.publish ─▶ board:{id}  ──▶  every page
```

- **What must be agreed on goes through the server.** The canvas fires `onCommit` with the changed elements; a flow
  sends them to `board-apply` (queued, so they are kept in order). The action validates them, merges them element by
  element, keeps the board, and announces what each element now IS on `board:{id}` — a channel declared
  `publish: 'server'`, which no page can speak on. The canvas treats that announcement as the confirmation.
- **What only has to be fast goes page to page.** The cursor, what is being dragged and what is selected travel on
  `room:{id}` twenty times a second, straight between pages. Nothing on it is kept.

Both roads run over **one WebSocket per page** (`realtime: { transport: 'websocket' }` in `main.ts`). Where a socket
cannot open — behind HTTP/2, or a proxy that drops upgrades — the page falls back to a Server-Sent Events stream.

**Conflicts** resolve per element, the same way on the server and every screen: a higher `version` wins, a tie goes to
the lower `nonce`. The server announces the winner, not the request, so a screen whose edit lost corrects itself the
moment the answer lands. Votes, a comment's replies and chat lines are not commits: each is added on the server, one
at a time, so two people at once both count. **A refused commit** fires `onFlowError`: the page calls the canvas's
`rollback`. **A dropped connection** fires `onResync` when it comes back, and the page reads the board again.

---

## What Plitzi does here, and what the components do

The canvas draws and takes the pointer; the share card and the copy texts ask the browser for the clipboard. Everything
else is authored.

| On screen                  | How                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| -------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| A board in the first paint | `apiContainer` with `runtime: 'server'` naming `board-load`, a `render` action reading the route's `{{id}}`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| The front page             | `board-list`, rendered on the server: the sandbox (the canvas with three played collaborators, `demo`), "Better together" (six scenes in CSS keyframes — an agent, a kanban, cursors, piles, reactions, the laser — still for `prefers-reduced-motion`), "Everything on one board", the templates, the featured boards and the public ones, searched by name; each thumbnail is the canvas in `mode: 'view'`                                                                                                                                                                                                                                                                                                                                                                                     |
| New board                  | `board-create` (with a `template`: kanban with real columns, brainstorm, retro, flowchart, mind map, roadmap, weekly sync — each a drawing of the elements above) answers an id; the flow navigates to it; it is kept for good. "Quick private board" makes one nobody finds on the front page, gone in a day                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| Private, temporary         | Board settings (the sliders beside the title) → "Who can find it" (public: listed; private: only with the link) and "How long it lasts" (an hour, five, a day, a week, for good): `board-reach`, announced as `reach`. The keys are stored with that lifetime, so the store forgets them; the header counts down, in the danger colour in its last hour, and sends everyone home at zero                                                                                                                                                                                                                                                                                                                                                                                                         |
| Tools                      | Down the left edge, built from one registry (`space/elements.ts`: every element with its category, icon, description and keys) — the basics a button each, every other category one button that shows the last thing picked from it and opens the rest. At its foot, + (I) opens All elements (`library.ts`): every element by category, searchable, with ★ favourites first. `state.tool` is set by the bar, the keys and the canvas's `onToolChange`                                                                                                                                                                                                                                                                                                                                           |
| Style panel                | Excalidraw's properties, each shown only for what the selection takes (`takesStyle` in `model.ts`): stroke, background, fill style (hachure, cross-hatch, solid), width, stroke style (solid, dashed, dotted), sloppiness (architect, artist, cartoonist), edges (sharp, round), opacity, the pen's brush, and layers. Opens with a selection; what a new element is drawn with is the last style chosen                                                                                                                                                                                                                                                                                                                                                                                         |
| Brushes                    | The pen draws in Pizarra's own ink, a Japanese brush that swells and tapers, a fountain pen, a marker, a see-through highlighter, a grainy pencil, chalk, or neon — `pens.ts`, one table of perfect-freehand options, and `draw.ts` for the grain and the glow. Selection and clicks reach as far as the ink does                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Frames and kanban          | The frame tool (F) draws a titled area; what is made or dropped in it is its member (`parent`) and moves with it — a click on its empty inside picks it. A column (the kanban group, or ☰ on a selected frame) stacks its members top to bottom; + beside a selected column adds one next to it, moving the rest along; the kanban group also drops a whole To do · Doing · Done board. Dragging over a frame lights it, and in a column shows the gap it would take; a card dragged out of every frame goes back to its column                                                                                                                                                                                                                                                                 |
| Cards                      | A task (C): its words, a colour strip, who wrote it, and a box that ticks it done                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Comments                   | Feedback pinned to a place (M): written in a composer beside the pin and posted on purpose — Post, or Enter; Escape drops it, and looking away leaves it waiting. A pin with its author's initial, its bubble open on hover; selected, its thread opens beside it, to answer (`onReply` → `board-reply`) and resolve                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| Chat                       | The comments icon: lines kept with the board (`board-chat`, the last hundred served with it), carried as `chat` on its channel, an unread count while it is closed, quick emoji. An agent's lines are marked AI                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Quick connect              | Click a shape's connection point: another like it, that way, joined by an arrow and ready to write in                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Connectors                 | An arrow or a line drawn from a shape leaves it by the side facing the other end — which follows the pointer — and fixes to the anchor it is let go on. Resolved every time it is drawn, so it follows a shape however it moves, on every screen                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Frames panel, presenting   | The list icon: the frames in reading order, each a click away; Present (or ▶ on a selected frame) goes through them one at a time with ← →, and every page on the board eases to each frame and says who is presenting                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| Minimap                    | The whole board in a corner, with everyone's view and cursor in their colour; a click or a drag goes there                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Authors                    | Notes, cards and comments keep who wrote them: "Author: …" on a card, and on a note while it is pointed at. You → "Show who wrote notes and cards" turns it off, for this person's screen                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Selection tools            | The canvas's authored CHILDREN, laid beside the selection: front, back, column, present, done, tidy up (a grid), group, vote, duplicate, delete. A hollow shape is picked up anywhere inside it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Copy and paste             | ⌘C / ⌘X / ⌘V copy elements — between boards too, as JSON behind a mark — and pasted text becomes a note                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Sticky pile                | The tray at the foot: the `stickyStack` hands a pile over on the pointer going down (`onPick`) and dragging from a pile on the board peels a note off it; stamps (`stamp`: an emoji put down as a text at 64, side by side when stamped again); the laser; cursor chat; the reactions                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Pictures                   | Paste an image: shown at once, committed once `board-upload` kept it (bytes checked, an asset id answered); the middleware in `main.ts` serves it with a random id, `nosniff` and a `default-src 'none'` CSP                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| Password                   | Board settings → Password (`board-lock`): says whether the board has one (a password is never shown back), Set / Change, and Remove password; a 🔒 beside the title while it has one. The length is checked in the form, since a refused run does not bring its reason back to the flow. A locked board renders only its name; `board-open` with the password answers its elements, a key every change carries and the topic its channels go by                                                                                                                                                                                                                                                                                                                                                  |
| Read-only boards           | The featured boards, and any board its creator makes read-only (Board settings → "Who can change it": everyone, or only me). `board-create` and `board-copy` answer an owner key, which the page keeps (`state.owned`) and every change carries; `board-readonly` takes only that key, and a read-only board lets only it through — announced as `readOnly`. Everyone else gets `mode: 'read'` (pan, zoom, laser, reactions, chat, follow), the chrome that changes a board hidden, every write refused. "Use as template" runs `board-copy`                                                                                                                                                                                                                                                     |
| Delete                     | The trash beside the title asks once; `board-delete` says `deleted` on the board's topic and every page on it goes home                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Popovers                   | You, Share, the timer, Delete, the shapes, the frames and the reactions close on Escape or a click outside. The chat is a panel, and stays open while you work                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| Text size                  | A text's handles resize its words: `fontSize` (8–480), measured again when let go; S / M / L in the style panel give it back to the stroke width                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Votes, timer, laser        | 👍 (⇧V) merged on the server; `board-timer` and the `countdown` element; the laser (K) is `laser: true` on the pointer messages                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Cursor chat, reactions     | `/` types at your cursor; the smiley in the tray opens twelve reactions, each with its own sound, which float up where you point, on every screen                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Bring everyone here        | `summon` sends this view on the room; every page moves to it and says who brought them — and this one says it was sent                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| Follow                     | Click an avatar: this page shows what they show until the person here touches the board                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Who is here                | A `channel` element on `room:{{ id }}` announcing `computed.me`; the avatars are a list over `room.members` — an agent's is a ✦                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| First paint                | `settings.paintedState` (each group's last pick, name, colour, minimap): kept in a cookie too, so the server draws the toolbar and avatar as they were left, and a reload swaps nothing in                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Keyboard                   | `onKey` flows: a key per tool, the arrows nudge (⇧ by ten) or step through a presentation, ⌘Z/⌘⇧Z, ⌘C/⌘V, ⌘D, ⌘G, `[`/`]`, ⌘`[`/⌘`]`, ⇧F to fit, `/`, ⇧V, Delete, `?`, Escape                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| Full screen, theme         | `toggleFullscreen` on the canvas (the page, chrome included); `themeToggle`, with the canvas's `--board-*` colours read again on `theme.resolved`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Sounds                     | Cues synthesised with Web Audio (`sounds.ts`, nothing to load), through a master level and a compressor. Under one's own hand: a note, a shape, a text or a picture put down, a pen stroke, an arrow connected, a card dropped in a column, a restyle, layers, grouping, tidying, copying, something removed, undo, a card done, a vote, a comment, a message sent, the laser, an export's shutter, a presentation opening and closing. From the others: arrivals and departures, chat, comments, answers, votes, tasks done, each reaction its own voice, being brought to a view, a presentation moving on. For everyone: a timer starting, ticking its last five seconds (`countdown`'s `onTick`), ending — or stopped by hand — a password set, a board deleted. You → Sounds turns them off |

---

## Agents

An agent joins a board the way a person does. [`src/agent`](./src/agent) is an MCP server, over stdio, that opens the
board through `board-open`, connects to its channels on the same socket a page opens, announces itself on the room
(`$presence` with `agent: true`), and commits through `board-apply`. Nothing on the server knows it is not a browser —
so it works against any Pizarra, one process or replicas behind a balancer.

```bash
claude mcp add pizarra -- node examples/06-full-examples/04-whiteboard/src/agent/main.ts
```

Then give it a board link (the ✦ beside Share opens "Invite an AI agent", which copies both lines). On the board, people see it arrive — a ✦
avatar, a cursor that glides to what it works on, its lines in the chat marked AI.

| Tool                                              | What it does                                                                                                       |
| ------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------ |
| `join_board` · `create_board` · `leave_board`     | Join by link (and password), or start one; answers the board as text                                               |
| `read_board`                                      | Every element with its id, type, text, place and frame; the connections; who is here; the recent chat              |
| `add_elements`                                    | Notes, cards, text, frames (columns too), comments and shapes, in one commit — placed in free space, or in a frame |
| `connect` · `update_elements` · `delete_elements` | Arrows between ids; text, place, colour, done, frame; removal that lets arrows go                                  |
| `say` · `point_at` · `react` · `reply_to_comment` | The chat and the cursor; the laser; an emoji; a comment's thread                                                   |
| `wait_for_activity`                               | Listens: chat lines, words at cursors, changes, arrivals — what makes a conversation                               |

`PIZARRA_URL` is where a bare id is looked for (`http://127.0.0.1:4016`), `PIZARRA_AGENT_NAME` and
`PIZARRA_AGENT_COLOR` who it is on the board (`Claude`, `orchid`).

---

## Several replicas

`deployment.ts` reads the environment. Without `REDIS_URL` everything is in this process's memory. With it, every
replica shares one Redis for the three things they must agree on:

```bash
REDIS_URL=redis://127.0.0.1:6379 BOARD_SECRET=… PORT=4016 yarn workspace @plitzi/example-whiteboard start
REDIS_URL=redis://127.0.0.1:6379 BOARD_SECRET=… PORT=4017 yarn workspace @plitzi/example-whiteboard start
```

- **The channels** — `createRedisPubSub`: a cursor moved by somebody connected to one replica is drawn on a page
  connected to another.
- **The boards and the pictures** — an `ActionKvAdapter` and an asset store over Redis. Every write takes a lock in the
  `kv` itself (the first `increment` of its key wins; it expires on its own), so two commits through two replicas
  never lose one of them — the difference between 42 and 90 of 90 commits kept in the test below.
- **The secret** — `BOARD_SECRET`, the same on every replica: a locked board's key and topic, handed out by one, are
  checked by whichever the next request reaches. A replica refuses to start without it.

Tested behind a round-robin balancer without affinity — every request and every socket to the next replica: three
browsers on three replicas see each other; 90 commits raced in parallel across them are all kept; a picture uploaded
through one is served by the others; a password set through one opens through another; chat lines and replies raced
across them are all kept; an agent joins through the balancer and hears the people on it.

One thing needs **affinity**: the Server-Sent Events fallback. A stream's publishes are posted to `/_realtime` with
the stream's token, which only the replica holding the stream knows. Pizarra runs on WebSockets, where a publish is a
frame on the socket and this never arises; a deployment that serves the stream must pin `/_realtime` to one replica.

---

## Taking it further

- **Boards that outlive Redis.** Hand `action.kv` and the asset store adapters that persist to disk or object storage.
- **Who may draw.** Every channel and action here is `public`, and a password is the only lock. `access: { mode:
'session' }` on both is the whole change for a signed-in board.

See [`docs/en/realtime.md`](../../../docs/en/realtime.md) for channels in general.
