/** Element ids other files address: named once, so a flow cannot aim at an element that was renamed. */
export const BOARD_ID = 'canvas';

/** The server provider holding the board: `apiContainer_board`, and `board.*` in a binding. */
export const BOARD_PROVIDER = 'board';

export const GALLERY_PROVIDER = 'gallery';
