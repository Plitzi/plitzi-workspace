node_modules
dist
!src/tests
build
.env
yarn-error.log
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions
.turbo
.claude
.cursor
.sdk-plugins
.mcp.json
opencode.json

# Playwright run output — screenshots and traces to look at, never baselines to compare against.
e2e/.artifacts
