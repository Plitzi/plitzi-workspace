// Packages
import { expect, describe, test } from '@jest/globals';

describe('Testing BuilderOverlayHelper', () => {
  test('calculateDistances', async done => {
    expect({}).toStrictEqual({});
    done();
  });
});
