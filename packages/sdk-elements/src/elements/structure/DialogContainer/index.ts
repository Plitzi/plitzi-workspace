import declaration from './declaration';
import BaseDialogContainer from './DialogContainer';

const DialogContainer = Object.assign(BaseDialogContainer, declaration);

export default DialogContainer;
