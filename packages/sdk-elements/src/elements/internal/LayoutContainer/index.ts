import declaration from './declaration';
import BaseLayoutContainer from './LayoutContainer';

const LayoutContainer = Object.assign(BaseLayoutContainer, declaration);

export default LayoutContainer;
