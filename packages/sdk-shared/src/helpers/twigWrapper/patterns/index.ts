import {
  TOKEN_REGEX,
  TOKEN_STRICT_REGEX,
  TOKEN_MATCH,
  TOKEN_INNER,
  FILTER_RE,
  IF_BLOCK,
  COMPARISON,
  STRING_LITERAL,
  FOR_OPEN,
  FOR_TAG,
  RANGE_EXPR,
  BREAK_TAG,
  CONTINUE_TAG
} from './patterns';

export * from './patterns';

export default {
  TOKEN_REGEX,
  TOKEN_STRICT_REGEX,
  TOKEN_MATCH,
  TOKEN_INNER,
  FILTER_RE,
  IF_BLOCK,
  COMPARISON,
  STRING_LITERAL,
  FOR_OPEN,
  FOR_TAG,
  RANGE_EXPR,
  BREAK_TAG,
  CONTINUE_TAG
};
