# Prose written and wrapped by hand: the docs and the agent skills. Their tables are laid out for reading in a plain
# editor, and a formatter run over one file re-aligns every table in it — someone else's sections included.
docs/**/*.md
packages/*/skills/**/*.md
